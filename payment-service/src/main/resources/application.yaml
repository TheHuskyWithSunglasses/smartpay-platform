spring:
  application:
    name: payment-service
  jpa:
    hibernate.ddl-auto: none
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/smartpay_payments
    username: smartpay
    password: example
  data:
    redis:
      host: localhost
      port: 6379
  liquibase:
    enabled: true
server:
  port: 8081